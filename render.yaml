services:
  - type: web
    name: notions
    runtime: docker
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: notions-db
          property: connectionString

databases:
  - name: notions-db
    plan: free
